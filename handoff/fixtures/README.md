# Fixtures and provenance

- restaurant-quality-reference.mp4 is the actual completed Kling 3.0 pro-mode, 10-second, sound-on result from this conversation; native container duration approximately 10.041667 seconds, 1744x1188.
- restaurant-source.jpeg is the user's attached source if present; visible copyright watermark must be preserved. Neither asset is cleared here for public promotion.
- motion-plan.json is a hand-authored structured example compatible with the new proposed director schema, not a claim that Astra returned it in a live test.
- provider-completed.json is synthetic, non-routable test data. Its example.test URL must never be used as a real provider output.
- The seven design boards contain illustrative dog/family/restaurant imagery. Isolated production photographs and the exact typeface were not supplied. Follow design-system guidance rather than pretending these are recovered source assets.
- Review mode balances are scenario fixtures. New customers do not automatically receive the pictured three credits.
