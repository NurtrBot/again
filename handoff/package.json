{
  "name": "again-implementation-handoff",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Offline reference-core verification, not the finished app.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "python3 tools/validate_package.py"
  }
}
