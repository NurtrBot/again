import 'server-only';
/* STUB — replaced by the real implementation in the backend workstream. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const filmsService: any = new Proxy({}, { get: () => async () => { throw new Error('filmsService not implemented yet'); } });
