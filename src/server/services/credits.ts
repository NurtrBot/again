import 'server-only';
/* STUB — replaced by the real implementation in the backend workstream. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const creditsService: any = new Proxy({}, { get: () => async () => { throw new Error('creditsService not implemented yet'); } });
